# =================================
# Within App - Environment Variables
# =================================

# Google Maps API Key (필수)
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Frontend URL (배포 시 도메인으로 변경)
FRONTEND_URL=http://localhost

# Google OAuth 설정
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost/api/auth/google/callback

# JWT Secret (보안을 위해 반드시 변경)
JWT_SECRET=your-super-secret-jwt-key-change-this

# Admin JWT Secret (보안을 위해 반드시 변경)
ADMIN_JWT_SECRET=your-admin-jwt-secret-change-this

# Session 유효기간 (시간)
SESSION_EXPIRY_HOURS=168

# PostgreSQL 설정
POSTGRES_USER=within
POSTGRES_PASSWORD=within
POSTGRES_DB=within

# S3 Storage (선택 - 사진 업로드)
# STORAGE_MODE: local (기본) 또는 s3
STORAGE_MODE=local
S3_BUCKET=within-eco
S3_REGION=ap-northeast-2

# 경기기후플랫폼 API (선택)
VITE_CLIMATE_API_KEY=your-climate-api-key
